# Implement backend access from frontend

- Write code to hit the backend protected endpoint from the frontend page
- Just do that and log the response in the console.
- Change keycloak configuration on Vue3 if necessary (I guess it's necesary to use bearer token to authenticate)
